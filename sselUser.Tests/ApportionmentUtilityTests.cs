using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace sselUser.Tests
{
    [TestClass]
    public class ApportionmentUtilityTests
    {
        [TestMethod]
        public void CanGetMinimumDays()
        {
            throw new NotImplementedException();

            //int actual;

            //actual = ServiceProvider.Current.Billing.Apportionment.GetMinimumDays(DateTime.Parse("2016-08-01"), 268, 154, 30);
            //Assert.AreEqual(8, actual);

            //actual = ServiceProvider.Current.Billing.Apportionment.GetMinimumDays(DateTime.Parse("2016-07-01"), 2691, 154, 17);
            //Assert.AreEqual(4, actual);

            //actual = ServiceProvider.Current.Billing.Apportionment.GetMinimumDays(DateTime.Parse("2016-09-01"), 96, 154, 17);
            //Assert.AreEqual(4, actual);
        }
    }
}
